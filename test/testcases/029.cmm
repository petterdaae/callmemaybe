outer = <a int> => int {
    innerOuter = <a int> => int {
        inner = <a int> => int {
            return a + 2
        }
        a = (call inner with a) + 2
        return a
    }
    a = (call innerOuter with a) + 2
    return a
}

a = call outer with 10
println a
