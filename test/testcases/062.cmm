printInts = | me, ints list<int, 5>, startIndex int | bool {
    if startIndex == 5 {
        return true
    }
    current = get startIndex from ints
    println current
    return call me with ints, startIndex + 1
}

ints = <int, 5>[1, 2, 3, 4, 5]
_ = call printInts with ints, 0
