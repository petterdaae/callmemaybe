printInts = | me, ints list<int>, startIndex int | bool {
    if startIndex == 5 {
        return true
    }
    current = ?ints[startIndex]
    println current
    return #me (ints, startIndex + 1)
}

ints = <int, 5>[1, 2, 3, 4, 5]
_ = #printInts(ints, 0)
